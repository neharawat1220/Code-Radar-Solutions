#include<stdio.h>
int main(){
    int is_prime(int number);
    scanf("%d",&num);
    if(is_prime(num)){
        printf("Prime");
    }else{
        printf("Not Prime");
    }
    return 0;
}